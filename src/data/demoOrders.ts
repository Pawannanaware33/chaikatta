import { OrderWithItems } from '../types/database.types';

/**
 * Demo orders are now completely cleared for real production usage.
 * Returns an empty array.
 */
export function getDemoHistoricalOrders(): OrderWithItems[] {
  return [];
}
